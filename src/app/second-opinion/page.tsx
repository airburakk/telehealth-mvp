import { SecondOpinionContent } from "./SecondOpinionContent";

export const metadata = { title: "Second Opinion · İkinci Görüş — AURA" };

export default function SecondOpinionPage() {
  return <SecondOpinionContent />;
}
